# linkbox 설계서 (DESIGN)

- 작성자: 문지원
- 작성일: 2026-08-18
- 근거 문서: `PLAN.md`, `PRD.md`, `prd_lite.md` / 작업 규칙: `CLAUDE.md`
- 선택한 설계 방식: 실용적 절충 (화면 1개 + 서버 기능 3개 + 공용 부품 5개)

## 1. 설계 목표

- 화면은 1개만 둔다. 저장하는 곳과 보는 곳이 같은 페이지에 있다.
- 비밀 키가 필요한 일(DB 접근, AI 호출)은 전부 서버에서 한다. 브라우저는 서버에만 말을 건다.
- 링크 1건당 AI 호출은 저장할 때 1회뿐이다.

---

## 2. 화면 구성

화면은 하나이고, 위에서 아래로 네 덩어리다.

```
┌────────────────────────────────────────────────────────┐
│  linkbox                                    (제목)      │
├────────────────────────────────────────────────────────┤
│  [ https://...      ] [비밀번호] [ 저장 ]   (저장 줄)   │
│   └ 1시간 안에 비밀번호를 통과했으면 비밀번호 칸은 숨김 │
├────────────────────────────────────────────────────────┤
│  [ 검색어 입력                    ] [ ✕ 지우기 ]        │
│  선택된 태그: #ai ✕                          (검색 줄)  │
├────────────────────────────────────────────────────────┤
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                │
│  │ 제목 2줄  │ │ 제목 2줄  │ │ 제목 2줄  │   (카드 격자) │
│  │ 요약 3줄  │ │ 요약 3줄  │ │ 요약 3줄  │                │
│  │ #태그 #태그│ │ #태그     │ │ #태그 #태그│               │
│  │ 2026-08-18│ │ 2026-08-18│ │ 2026-08-18│      🗑        │
│  └──────────┘ └──────────┘ └──────────┘                │
│  ┌──────────┐ ┌──────────┐                             │
│  │ ...       │ │ ...       │                            │
│  └──────────┘ └──────────┘                             │
│                                                        │
│                  [ 더 보기 ]                            │
└────────────────────────────────────────────────────────┘
```

### 2.1 각 요소가 하는 일

| 위치 | 요소 | 하는 일 |
|---|---|---|
| 저장 줄 | 주소 입력칸 | URL을 붙여넣는 곳. 비어 있으면 저장 버튼이 눌리지 않는다. |
| 저장 줄 | 비밀번호 칸 | 1시간 안에 통과한 적이 있으면 아예 보이지 않는다. |
| 저장 줄 | 저장 버튼 | 처리 중에는 눌리지 않고 "저장 중..."으로 바뀐다. |
| 저장 줄 | 안내 문구 자리 | "이미 저장된 링크입니다", "올바른 주소를 입력해 주세요" 등이 여기 뜬다. |
| 검색 줄 | 검색 입력칸 | 제목·요약·태그를 함께 뒤진다. |
| 검색 줄 | 선택된 태그 | 카드의 태그를 클릭하면 여기 표시되고, ✕로 해제한다. |
| 카드 격자 | 카드 | 제목(2줄), 요약(3줄), 태그, 저장일. 넘치면 말줄임(…). 카드를 클릭하면 원문이 새 탭에서 열린다. 새 탭이라 현재 탭의 검색어와 스크롤 위치는 그대로 유지된다. |
| 카드 격자 | 삭제 버튼 | 카드 안 우측. 누르면 "정말 삭제할까요?"를 먼저 묻는다. 아직 비밀번호를 통과하지 않았으면 이 확인창에서 비밀번호도 함께 입력받는다. 태그와 삭제 버튼 클릭은 카드 클릭보다 우선하며 원문 새 탭을 열지 않는다. |
| 맨 아래 | 더 보기 | 다음 20개를 이어서 불러온다. 남은 게 없으면 버튼이 사라진다. |

### 2.2 화면이 비어 있을 때

| 상황 | 보이는 것 |
|---|---|
| 저장된 링크 0개 | 일러스트 1장 + "아직 저장된 링크가 없습니다. 위에 주소를 붙여넣어 보세요." |
| 검색 결과 0건 | "찾는 검색어와 일치하는 링크가 없습니다." + 검색 지우기 버튼 |

### 2.3 크기와 색

- 본문 최대 폭 1080px, 가운데 정렬. 카드는 기본 3열이고 화면이 좁아지면 2열 → 1열.
- 카드 한 칸 최소 폭 280px, 높이는 전부 같게 고정.
- 색은 3개뿐이다. 배경 흰색, 글자 `#1F2937`, 강조 `#2563EB`. CSS 변수로 한 곳에서 관리한다.

---

## 3. 데이터 흐름 (입력 → 처리 → 출력)

### 3.1 링크 저장

```
[입력] 사용자가 URL을 붙여넣고 저장 클릭
   ↓
[브라우저] 저장 버튼 잠그고 "저장 중..." 표시 → 서버에 URL과 비밀번호 전달
   ↓
[서버] ① 비밀번호 확인      → 틀리면 여기서 중단 (AI 호출 없음)
       ② 주소 형식 검사     → 형식이 아니면 중단
       ③ https:// 보완, 주소 정규화 (호스트 소문자, 끝 슬래시와 # 뒤 조각 제거, utm_·fbclid·gclid·ref 제거)
       ④ 이미 있는 주소인지 확인 → 있으면 중단하고 기존 카드 안내
       ⑤ 페이지 가져오기 (15초 넘으면 포기)
       ⑥ 본문만 뽑아내기 (앞 5,000자까지)
       ⑦ 본문이 200자 미만이면 AI 건너뜀
       ⑧ AI에 요약·태그 요청 (여기서 딱 1회)
       ⑨ 60자 넘는 문장은 60자에서 자르고 말줄임(…) 붙이기, 태그가 3개를 넘으면 앞 3개만 남기기
       ⑩ DB에 저장
   ↓
[출력] 새 카드가 목록 맨 앞에 추가되고, 저장 버튼이 원래대로 돌아온다
```

**중간에 멈춰도 결과는 남는다** — ⑤에서 실패하면 `요약 실패`, ⑦이면 `요약 없음`으로 저장되고 URL과 제목은 보존된다. ①~④에서 멈추면 아무것도 저장되지 않는다.

### 3.2 목록 보기

```
[입력] 화면 열기 (또는 "더 보기" 클릭)
   ↓
[서버] 첫 화면은 서버 컴포넌트가 lib/db.ts로 DB를 직접 읽어 최신순 20개를 그린다.
       "더 보기"부터는 GET /api/links로 그다음 20개를 가져온다. (둘 다 브라우저가 DB에 직접 붙지 않는다)
   ↓
[출력] 카드 격자에 표시. 0개면 안내 문구.
```

### 3.3 검색 / 태그 필터

```
[입력] 검색어 입력, 또는 카드의 태그 클릭
   ↓
[서버] search_links 함수를 호출한다 — 제목·요약·태그 세 곳에서 글자가 들어 있는지 확인 (대소문자 무시)
       tags가 배열이라 ILIKE를 직접 걸 수 없어 DB 함수로 처리한다. 검색어와 태그가 함께 오면 둘 다 만족하는 것만 남긴다(AND).
       ※ 이미 불러온 20개 안에서 거르지 않는다. 그래서 21번째 이후도 찾힌다.
   ↓
[출력] 걸러진 카드 20개씩 표시. 0건이면 안내 문구 + 지우기 버튼.
```

### 3.4 삭제

```
[입력] 카드의 삭제 버튼 클릭 → "정말 삭제할까요?" 확인
   ↓
[서버] 비밀번호 확인 (1시간 서명 쿠키가 유효하면 생략) → DB에서 해당 줄 삭제
   ↓
[출력] 목록에서 카드가 사라진다
```

### 3.5 비밀번호 확인 흐름 (저장·삭제 공용)

```
비밀번호 입력 → 맞음 → 서버가 서명한 쿠키(HttpOnly)를 1시간 만료로 발급 → 이후 1시간 동안 안 물어봄
              → 틀림 → login_attempts 테이블에 IP 기준으로 실패 횟수 +1 → 5회가 되면 10분간 저장·삭제 모두 차단
```

---

## 4. 기술 선택

### 4.1 기본 — Next.js (고정)

화면과 서버 기능을 한 프로젝트에서 만들 수 있고, 완성 후 Vercel에 그대로 올릴 수 있어 Part 5 배포까지 이어진다. 언어는 TypeScript를 쓴다.

### 4.2 추가로 설치할 도구 3개

| 도구 | 쉬운 설명 (한 줄) | 없으면 생기는 일 |
|---|---|---|
| **@supabase/supabase-js** | Next.js가 Supabase 데이터베이스에 링크를 저장하고 꺼내오게 해주는 공식 연결기다. | DB에 접근할 방법이 없다 |
| **openai** | OpenAI에게 "이 글을 3줄로 요약해줘"라고 부탁하고 답을 받아오는 공식 호출기다. | 요약과 태그를 만들 수 없다 |
| **cheerio** | 웹페이지에서 광고·메뉴·버튼을 걷어내고 본문 글자만 뽑아내는 도구다. | AI가 메뉴 글자까지 요약해버린다 |

### 4.3 일부러 쓰지 않는 것

| 안 쓰는 것 | 이유 |
|---|---|
| Tailwind 같은 디자인 도구 | 색이 3개, 화면이 1개뿐이라 기본 CSS로 충분하다 |
| 로그인·인증 라이브러리 | 계정을 만들지 않기로 했다. 비밀번호 1개면 된다 |
| 검색 엔진 | 연 1,200건 규모라 DB 기본 검색으로 충분하다 |
| 상태 관리 라이브러리 | 화면이 1개라 관리할 상태가 적다 |

---

## 5. 파일 구조 (실용적 절충안)

```
my-app/
├── app/
│   ├── page.tsx              화면 1개 (저장 줄 + 검색 줄 + 카드 격자)
│   ├── globals.css           색 변수 3개, 격자 레이아웃
│   └── api/
│       ├── links/route.ts        저장(POST) + 목록·검색(GET)
│       └── links/[id]/route.ts   삭제(DELETE)
├── components/
│   ├── LinkCard.tsx          카드 1개 (목록·검색·필터가 전부 이걸 씀)
│   ├── SaveForm.tsx          저장 줄 (주소 입력칸, 비밀번호 칸, 저장 버튼)
│   ├── SearchBar.tsx         검색 줄 (검색 입력칸)
│   └── LinkList.tsx          카드 격자 + "더 보기"
└── lib/
    ├── db.ts                 Supabase 연결
    ├── ai.ts                 요약·태그 생성
    ├── fetchPage.ts          페이지 가져오기 + 본문 추출
    ├── buildLink.ts          저장할 한 줄 만들기 (가져오기 → 요약 → 실패 처리)
    ├── auth.ts               비밀번호 확인, 서명 쿠키 발급·검증(1시간), 5회 차단
    └── constants.ts          고정 숫자 (15초, 5000자, 200자, 60자, 태그 3개, 20개)
```

`lib/`의 5개 파일이 이 설계의 핵심이다. 규칙에 걸린 숫자와 판단을 여기 모아두면, 화면이나 API를 고쳐도 규칙이 흐트러지지 않는다.

**카드 격자를 `LinkList.tsx`로 옮긴 이유** — "더 보기"는 이미 보고 있는 카드 아래에 다음 20개를 **이어붙인다**. 이어붙이려면 지금까지 받은 목록을 브라우저가 들고 있어야 해서 클라이언트 컴포넌트가 필요하다. 첫 20개는 서버가 그려서 넘겨주므로 첫 화면 속도는 그대로다. 검색어나 태그가 바뀌면 이어붙이던 것을 버리고 처음부터 다시 센다.

**검색어를 주소(`/?q=...`)에 담는 이유** — 검색은 서버가 DB에 질의해 처리한다. 검색어를 주소에 담아 두면 화면을 다시 그릴 때 서버가 그 값을 읽어 질의하므로, 브라우저가 이미 받은 20개 안에서 거르는 일이 생기지 않는다. 새로고침하거나 뒤로 가도 검색 상태가 그대로 남는 것은 덤이다.

**저장 규칙을 `buildLink.ts`로 모은 이유** — "언제 AI를 부르고, 못 부를 때 무엇을 저장하는가"는 규칙에 걸린 판단이라 `lib/`에 둔다. Route Handler에는 HTTP 처리만 남긴다. AI 호출이 이 파일 한 곳에서만 일어나므로, 링크 1건당 1회 규칙을 눈으로 확인하기도 쉽다.

**저장 줄을 `page.tsx`에서 떼어낸 이유** — `page.tsx`는 DB를 읽는 서버 컴포넌트라 브라우저에서 도는 코드를 함께 둘 수 없다. 저장 줄은 입력값과 "저장 중..." 상태를 브라우저에서 다뤄야 하므로 `SaveForm.tsx`로 분리한다. 목록 카드를 컴포넌트 1개로만 만든다는 규칙은 카드에 대한 것이고, 여기에 해당하지 않는다.

**비밀번호 칸을 숨기는 기준** — 쿠키는 HttpOnly라 브라우저 스크립트가 읽지 못한다. 그래서 `page.tsx`가 서버에서 쿠키를 확인해 통과 여부(true/false)만 `SaveForm`에 넘겨 칸을 숨긴다. 이것은 화면 표시용일 뿐이고, 저장·삭제 API는 여전히 비밀번호 값이나 유효한 서명 쿠키를 직접 확인한다.

---

## 6. 서버 기능 명세

| 하는 일 | 요청 | 주는 것 | 실패할 때 |
|---|---|---|---|
| 링크 저장 | `POST /api/links` (url, 비밀번호) | 저장된 카드 1건 | 401 비밀번호 틀림 / 400 주소 형식 오류 / 409 이미 있음 / 429 차단 중 |
| 목록·검색 | `GET /api/links?q=&tag=&offset=` | 카드 20건 + 더 있는지 여부 | 500 서버 오류 |
| 삭제 | `DELETE /api/links/{id}` (비밀번호) | 삭제 완료 | 401 비밀번호 틀림 / 404 없는 링크 / 429 차단 중 |


세 기능 모두 비밀번호 값 또는 유효한 서명 쿠키(1시간) 중 하나를 요구한다. 브라우저에 남은 표시만으로는 통과시키지 않는다.

**주고받는 형식** — 화면과 서버가 어긋나지 않도록 아래 형태로 고정한다.

| 상황 | 형태 |
|---|---|
| 성공 (저장, 삭제) | `{ "data": { ...카드 1건 } }` |
| 성공 (목록, 검색) | `{ "data": [ ...카드 ], "hasMore": true 또는 false }` |
| 실패 | `{ "error": { "code": "WRONG_PASSWORD", "message": "비밀번호가 맞지 않습니다" } }` |

화면에 띄우는 안내 문구는 서버가 `message`로 내려주고, 문구 원문은 `lib/constants.ts`에 모아 서버와 화면이 같은 것을 쓴다.

---

## 7. 데이터 모델

`links` 테이블과, 차단 기록용 `login_attempts` 테이블(ip, 실패 횟수, 마지막 실패 시각, 차단 해제 시각)을 둔다. 검색은 `search_links(검색어, 태그, 시작 위치)` 함수로 처리한다. 모든 테이블은 RLS를 켜고 서버 전용 키로만 접근한다.

| 칼럼 | 형태 | 내용 |
|---|---|---|
| `id` | 자동 번호 | 기본키 |
| `url` | 문자열 (고유값) | 정규화한 주소 (호스트 소문자, 끝 슬래시와 # 뒤 조각 제거, utm_·fbclid·gclid·ref 제거) |
| `title` | 문자열 | 페이지 제목. 비어 있으면 AI가 요약과 같은 1회 호출에서 함께 짓고, 페이지를 아예 못 읽었으면 URL의 호스트+경로를 넣는다 |
| `summary` | 문자열 | 문장 사이를 줄바꿈 문자로 이어붙인 단일 문자열. 또는 "요약 실패" / "요약 없음(본문이 짧은 페이지)" |
| `tags` | 문자열 목록 (text 배열) | 최대 3개, 공백 없이 10자 이내, 영어는 소문자. 빈 값 불가, 기본값은 빈 배열이며 AI 미호출 시 빈 배열 |
| `created_at` | 시각 (timestamptz) | 저장 시각. 화면에는 한국 시간(Asia/Seoul) 기준 2026-08-18 형태로 표시 |

---

## 8. 오류 처리

| 사용자가 겪는 상황 | 화면에 보이는 것 | 뒤에서 일어나는 일 |
|---|---|---|
| 주소가 아닌 값 입력 | "올바른 주소를 입력해 주세요" | 저장·AI 호출 모두 안 함 |
| 이미 저장한 링크 | "이미 저장된 링크입니다" + 기존 카드 강조 | AI 호출 안 함 |
| 비밀번호 틀림 | "비밀번호가 맞지 않습니다" | 실패 횟수 +1 |
| 5회 틀림 | "잠시 후 다시 시도해 주세요 (10분)" | 저장·삭제 모두 차단 |
| 페이지가 안 열림 / 15초 초과 | 카드에 "요약 실패" | URL·제목은 저장됨 |
| 영상·PDF 링크 | 카드에 "요약 없음(본문이 짧은 페이지)" | AI 호출 안 함 |
| 저장 중 인터넷 끊김 | 저장 버튼이 원래대로 돌아오고 안내 문구 | 저장 안 됨 (다시 시도하면 됨) |

---

## 9. 확인 방법

각 작업을 끝낼 때 `CLAUDE.md`의 검증 루프대로 브라우저에서 직접 확인한다. 특히 아래 4가지는 눈으로 봐야 한다.

1. 링크 25건 이상 저장한 뒤, 21번째 이후 링크가 검색되는가
2. 브라우저 개발자도구에서 비밀 키가 보이지 않는가
3. 링크 1건 저장에 AI 호출이 1회인가 (OpenAI 대시보드)
4. 실패하는 링크 3종(404, 유튜브, PDF)을 저장해도 앱이 멈추지 않는가
