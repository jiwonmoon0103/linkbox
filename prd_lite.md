# linkbox 서비스 기획서 (PRD_lite)

- 작성자: 문지원
- 작성일: 2026-08-18

---

## 1. 이 앱은 한 마디로 무엇인가요?
- 답변: 나중에 볼 링크를 붙여넣기만 하면, AI가 요약과 태그를 붙여 정리해주는 개인용 링크 보관함

---

## 2. 누가 쓰고, 왜 쓰나요? (딱 한 줄씩만!)
- 누가 쓰나요?
  - 답변: 링크를 저장하는 사람은 나 하나이고, 주소를 아는 사람은 누구나 로그인 없이 목록을 볼 수 있다.
- 어떤 불편을 해결하나요?
  - 답변: 저장은 해뒀는데 제목만 봐서는 무슨 내용인지 몰라, 결국 다시 열어보지 않게 되는 문제

---

## 3. 개발할 핵심 기능 (딱 2가지만!)
> 💡 너무 많은 기능을 만들려고 하면 AI가 코드를 꼬아버립니다.
> 가장 중요한 2개 기능만 "규칙"을 정해 알려주세요.

### 1) 링크 저장 + AI 자동 요약
- 설명: URL을 붙여넣으면 페이지를 읽어 제목, 3줄 요약, 태그를 자동으로 붙여 저장한다.
- AI가 꼭 지켜야 할 규칙:
  - 요약은 한국어 3문장 이내, 각 문장 60자 이내로만 생성한다.
  - 태그는 최대 3개까지, 각 태그는 한 단어로 만든다.
  - 페이지를 읽지 못하면(404, 로그인 필요 등) 요약을 지어내지 않고 요약란을 "요약 실패"로 남긴 뒤 URL은 그대로 저장한다.
  - AI 호출은 저장 시점에 1회만 한다. 목록을 볼 때는 저장된 요약만 읽고 다시 호출하지 않는다.
  - 저장 요청은 .env의 ADMIN_PASSWORD와 입력값이 일치할 때만 처리한다. 일치하지 않으면 AI를 호출하지 않고 거절한다.

### 2) 목록 보기 + 검색
- 설명: 저장된 링크를 최신순으로 보여주고, 키워드나 태그로 걸러서 찾는다.
- AI가 꼭 지켜야 할 규칙:
  - 검색은 DB 텍스트 검색만 사용하고 AI를 호출하지 않는다.
  - 목록은 한 번에 20개까지만 불러오고, 나머지는 "더 보기"로 가져온다.
  - 목록 카드에는 제목, 요약, 태그, 저장일만 표시한다. 제목은 2줄, 요약은 3줄까지만 보여주고 넘치면 말줄임(…) 처리한다.

---

## 4. 이번에 절대 만들지 않을 기능 (욕심 버리기)
> 💡 AI에게 미리 "이건 안 만들 거야"라고 선언해야 엉뚱한 코딩을 안 합니다.
> (아래는 "이번 판에서 안 만든다"는 뜻이며, 다음 단계에서 얼마든지 추가할 수 있다.)
- 공유 링크 발급: 사이트 자체가 공개이므로 별도 공유 링크 발급 기능은 만들지 않는다.
- 회원가입, 로그인: 계정 개념은 만들지 않는다. 저장 기능만 .env의 비밀번호 1개(ADMIN_PASSWORD)로 잠그고, 가입 화면이나 사용자 테이블, user_id 칼럼은 두지 않는다.
- 폴더, 즐겨찾기, 수동 태그 편집: 분류는 AI 자동 태그 3개로만 처리한다.
- 모바일 앱, 브라우저 확장 프로그램: 웹 화면 하나만 만든다.
- 저장된 요약 재생성 버튼: AI는 저장 시점 1회만 호출한다는 규칙과 충돌하므로 만들지 않는다.
- 링크 미리보기 이미지(썸네일) 수집: 목록 카드는 텍스트만 보여준다.

---

## 5. 디자인 느낌과 색상 고르기
- 전체적인 분위기: 군더더기 없는 정보 위주. 카드를 바둑판(격자) 형태로 나열한다. 애니메이션과 그림자는 쓰지 않되, 저장된 링크가 0개일 때 보여줄 일러스트 1장은 허용한다. 화면을 더 꾸미는 작업은 링크가 30개쯤 쌓인 뒤에 다시 정한다.
- 메인 색상: 배경 흰색, 글자 진회색(#1F2937), 강조 남색(#2563EB). 색은 3개를 넘기지 않고 CSS 변수로 한 곳에서 관리한다.
- 화면 크기 제약: PC 브라우저 기준 1개 화면. 본문 최대 폭 1080px, 가운데 정렬. 카드는 기본 3열이고 화면이 좁아지면 2열, 1열로 자동으로 줄인다. 카드 한 칸의 최소 폭은 280px, 높이는 모두 같게 고정한다. 목록 카드는 컴포넌트 1개로만 만든다.
