# linkbox 서비스 기획서 (PRD)

- 작성자: 문지원
- 작성일: 2026-08-18

---

## 1. 추진 배경 (문제 정의)
*누가, 어떤 업무에서, 어떤 불편을 겪는가*

- 작성자 본인이 나중에 볼 링크를 꾸준히 저장한다. 저장량은 일정하지 않아 적을 때는 하루 1개, 많을 때는 15개쯤 되며 한 달 기준으로는 100개 안팎이다. 그러나 지금의 북마크·메모 방식에서는 저장해둔 링크를 다시 찾아 열어보는 일이 거의 없다.
- 저장된 항목에 페이지 제목만 남아 있어, 목록을 봐도 그 링크가 어떤 내용이었는지 떠오르지 않기 때문이다. 결국 링크는 쌓이기만 하고 활용되지 않는다.
- linkbox는 URL을 붙여넣기만 하면 AI가 요약과 태그를 자동으로 붙여 정리해주는 개인용 링크 보관함으로, "저장은 했지만 다시 못 찾는" 문제를 해결한다.
- 링크를 저장하는 사람은 작성자 한 명이고, 주소를 아는 사람은 누구나 로그인 없이 목록을 볼 수 있다.

---

## 2. 현행 방식과 한계
*지금은 이 일을 어떻게 처리하고 있고, 무엇이 아쉬운가*

- 현행 방식: 브라우저 북마크에 추가하거나, 메신저의 "나에게 보내기"나 메모 앱에 URL만 붙여넣어 보관하고 있다.
- 한계 1 (내용 파악 불가): 저장된 것은 URL과 페이지 제목뿐이라, 목록만 보고는 내용을 알 수 없어 하나씩 다시 열어봐야 한다.
- 한계 2 (검색 불가): 제목에 들어 있는 단어를 정확히 기억해야만 찾을 수 있고, 내용 기준으로는 검색되지 않는다.
- 한계 3 (분류가 수동): 폴더나 태그를 직접 만들어 정리해야 해서 손이 많이 가고, 결국 정리하지 않게 된다.
- 한계 4 (쌓이기만 함): 저장은 쉽지만 다시 꺼내 보는 경로가 없어 보관함이 사실상 방치된다.

---

## 3. 목표와 기대효과 (성공 기준)
*측정 가능한 숫자로 적는다 (처리 시간, 문의 건수, 정답률 등)*

- 링크 1건을 저장하는 데 걸리는 시간: **5초 이내** (URL 붙여넣기 + 저장 클릭 1회 기준) — 측정: 직접 저장해보며 시간을 잰다.
- 저장한 링크를 다시 찾는 데 걸리는 시간: **10초 이내** (검색어 입력부터 해당 카드를 찾을 때까지) — 측정: 검색어를 입력해 카드를 찾을 때까지 직접 시간을 잰다. (찾기 쉬워지면 저장한 링크를 실제로 다시 열어보게 된다는 가정에 따른 대리 지표다.)
- AI 요약 생성 성공률: **90% 이상** (실패한 건은 "요약 실패"로 표시되고 URL은 보존) — 측정: 저장된 카드 중 "요약 실패"로 표시된 개수를 센다.
- 링크 1건당 AI 호출 횟수: **정확히 1회** (목록 조회와 검색 시 AI 호출 0회) — 측정: OpenAI 대시보드의 호출 건수를 저장한 링크 수와 비교한다.
- 목록 첫 화면 표시 시간: **2초 이내** (20개 기준) — 측정: 브라우저에서 새로고침해 목록이 보일 때까지 잰다.

---

## 4. 사용자와 이용 흐름
*누가 쓰는지와 사용 순서를 화살표로 적는다*

- 사용자
  - 저장자: 작성자 본인 1명. ADMIN_PASSWORD를 아는 사람만 저장할 수 있다.
  - 열람자: 사이트 주소를 아는 누구나. 로그인 없이 목록 보기와 검색만 가능하다.
- 이용 흐름 (저장): 볼 만한 페이지에서 URL 복사 → linkbox 접속 → 입력창에 붙여넣기 → 비밀번호 입력(1시간 안에 이미 통과했으면 건너뜀) → 저장 클릭 → 페이지 제목 가져오기와 AI 요약·태그 생성 → 목록 맨 앞에 카드 추가
- 이용 흐름 (열람): linkbox 접속 → 격자 목록 훑어보기 → 키워드나 태그로 검색 → 카드에서 요약 확인 → 원문 링크 열기
- 이용 흐름 (저장이 막히는 경우): 주소 형식이 아님 → "올바른 주소를 입력해 주세요" 안내 후 중단 / 이미 저장된 링크 → "이미 저장된 링크입니다" 안내 후 기존 카드 표시 / 비밀번호 5회 연속 불일치 → 10분간 저장 차단. 세 경우 모두 AI를 호출하지 않는다.
- 이용 흐름 (요약이 안 되는 경우): 페이지를 읽지 못하거나(404, 로그인 필요 등) 15초를 넘김 → 요약란에 "요약 실패" 표시 / 본문이 200자 미만 → 요약란에 "요약 없음(본문이 짧은 페이지)" 표시. 두 경우 모두 URL과 제목은 그대로 저장되어 목록에 남는다.

---

## 5. 주요 기능
*반드시 있어야 하는 기능(must)과 있으면 좋은 기능(nice)으로 구분한다*

### must

**1) 링크 저장 + AI 자동 요약**
- 설명: URL을 붙여넣으면 페이지를 읽어 제목, 3줄 요약, 태그를 붙여 저장한다. 제목은 페이지에 원래 있는 제목을 그대로 가져오고, 요약과 태그는 AI가 만든다.
- AI가 꼭 지켜야 할 규칙:
  - 요약은 한국어 3문장 이내, 각 문장 60자 이내로만 생성한다. 60자를 넘긴 문장은 60자에서 자르고 말줄임(…)을 붙여 저장하며, 다시 생성하지 않는다.
  - 태그는 최대 3개까지, 각 태그는 공백 없이 10자 이내로 만든다. 영어 태그는 모두 소문자로 통일한다.
  - 페이지를 읽지 못하면(404, 로그인 필요 등) 요약을 지어내지 않고 요약란을 "요약 실패"로 남긴 뒤 URL은 그대로 저장한다.
  - 페이지를 가져올 때 15초 안에 응답이 없으면 중단하고 "요약 실패"로 처리한다. 무한정 기다리지 않는다.
  - 본문은 앞에서부터 5,000자까지만 잘라 AI에 넘긴다. 그보다 긴 페이지도 5,000자까지만 사용한다.
  - 걷어낸 본문이 200자 미만이면(영상, PDF, 이미지 위주 페이지 등) AI를 호출하지 않고 요약란을 "요약 없음(본문이 짧은 페이지)"으로 저장한다. 껍데기 글자만 보고 요약을 지어내지 않는다.
  - 요약과 태그 생성에는 저렴한 등급 모델인 gpt-4o-mini를 사용한다. (더 나은 선택지가 생기면 이 줄만 고친다)
  - 제목은 페이지의 title을 그대로 사용한다. title이 비어 있거나 "홈"처럼 내용을 알 수 없는 경우에만 AI가 요약을 근거로 짧게 짓는다.
  - 페이지를 아예 읽지 못해 title도 요약도 없으면, 정규화된 URL의 호스트와 경로를 제목으로 넣고 AI를 호출하지 않는다. 제목을 AI가 지어야 하는 경우에도 요약과 같은 1회 호출 안에서 함께 받는다.
  - AI 호출은 저장 시점에 1회만 한다. 목록을 볼 때는 저장된 요약만 읽고 다시 호출하지 않는다.
  - 저장 요청은 .env의 ADMIN_PASSWORD와 입력값이 일치할 때만 처리한다. 일치하지 않으면 AI를 호출하지 않고 거절한다.
  - 비밀번호를 5회 연속 틀리면 같은 접속자의 저장 시도를 10분간 차단한다. 차단 중에는 AI를 호출하지 않는다. 접속자는 IP 주소로 구분하고, 실패 횟수와 차단 해제 시각은 login_attempts 테이블에 기록한다.
  - 비밀번호를 한 번 통과하면 서버가 서명한 쿠키(HttpOnly)를 1시간 만료로 내려주고, 그동안은 다시 묻지 않는다. 저장과 삭제 API는 비밀번호 값 또는 유효한 서명 쿠키 중 하나를 반드시 요구하며, 브라우저에 저장된 표시만으로는 통과시키지 않는다. 계정이나 사용자 테이블은 만들지 않는다.
  - 저장한 링크는 카드의 삭제 버튼으로 지울 수 있다. 삭제할 때도 ADMIN_PASSWORD를 확인하고, 일치하지 않으면 삭제하지 않는다. 지우기 직전에 "정말 삭제할까요?" 확인을 한 번 받으며, 5회 연속 실패 시 10분 차단 규칙도 삭제에 똑같이 적용한다. 아직 통과하지 않은 상태에서 삭제를 누르면 확인창에서 비밀번호를 함께 입력받는다.
  - 이미 저장된 URL이면 새로 저장하지 않고 "이미 저장된 링크입니다"로 알린 뒤 기존 카드를 보여준다. 이때 AI를 호출하지 않는다.
  - 중복 여부를 비교할 때는 utm_source 등 추적용 파라미터를 제거한 주소를 기준으로 삼는다.
  - 저장 전에 입력값이 주소 형식인지 검사한다. 형식에 맞지 않으면 AI를 호출하지도 저장하지도 않고 "올바른 주소를 입력해 주세요"라고 알린다.
  - https:// 가 빠진 주소는 자동으로 붙여서 시도한다. 입력창이 비어 있으면 저장 버튼이 동작하지 않는다.
  - 저장 처리 중에는 저장 버튼을 비활성화하고 "저장 중..."으로 표시한다. 처리가 끝나면 원래 상태로 돌아온다.

**2) 목록 보기 + 검색**
- 설명: 저장된 링크를 최신순으로 보여주고, 키워드나 태그로 걸러서 찾는다.
- AI가 꼭 지켜야 할 규칙:
  - 검색은 DB 텍스트 검색만 사용하고 AI를 호출하지 않는다.
  - 검색은 제목, 요약, 태그 세 곳을 함께 뒤진다. 셋 중 어디에든 검색어가 들어 있으면 결과에 포함한다.
  - 검색과 태그 필터는 서버에서 DB에 질의해 처리한다. 이미 불러온 20개 안에서만 거르지 않으므로, 21번째 이후에 저장한 링크도 검색된다. 검색 결과 역시 최신순으로 20개씩 "더 보기"로 가져온다.
  - 검색은 글자가 들어 있는지 확인하는 방식(ILIKE 부분 일치)을 쓰고 대소문자를 구분하지 않는다. 검색어와 태그가 함께 있으면 두 조건을 모두 만족하는 링크만 보여준다(AND). 단어 단위로 쪼개는 전문 검색은 쓰지 않는다. (한국어는 조사가 붙어 "머신러닝"으로 "머신러닝을"을 찾지 못하는 문제가 생기기 때문)
  - 카드의 태그를 클릭하면 그 태그가 붙은 링크만 걸러서 보여준다. 태그와 삭제 버튼 클릭은 카드 클릭보다 우선하며, 이때 원문 새 탭은 열리지 않는다.
  - 목록은 한 번에 20개까지만 불러오고, 나머지는 "더 보기"로 가져온다.
  - 목록 카드에는 제목, 요약, 태그, 저장일만 표시한다. 제목은 2줄, 요약은 3줄까지만 보여주고 넘치면 말줄임(…) 처리한다.
  - 저장일은 2026-08-18 형태로 표시한다. "3일 전" 같은 상대 표기는 쓰지 않는다.
  - 카드를 클릭하면 원문 링크를 새 탭에서 연다. 목록으로 돌아왔을 때 검색어와 스크롤 위치가 그대로 남게 한다.
  - 저장된 링크가 하나도 없으면 "아직 저장된 링크가 없습니다. 위에 주소를 붙여넣어 보세요."를 보여준다.
  - 검색 결과가 0건이면 "찾는 검색어와 일치하는 링크가 없습니다."와 검색 지우기 버튼을 함께 보여준다. 링크가 0개인 경우와 구분해서 표시한다.

### nice
- 없음. 이번 범위에서는 must 2개만 만든다. 기능을 늘리면 AI가 코드를 꼬기 때문에, 추가 후보는 모두 아래 6번 비범위에 두고 다음 단계에서 다시 검토한다.

---

## 6. 범위와 비범위
*이번 실습에서 만들 것과 만들지 않을 것을 명확히 가른다*

### 만들 것 (범위)
- 링크 저장 + AI 자동 요약 (URL 입력 → 제목·3줄 요약·태그 최대 3개 생성 후 저장)
- 목록 보기 + 검색 (최신순 격자 목록, 20개씩 더 보기, 제목·요약·태그 통합 검색, 태그 클릭 필터)
- 저장 기능의 비밀번호 확인 (.env의 ADMIN_PASSWORD 1개)
- 저장한 링크 삭제 (카드의 삭제 버튼, 삭제 시에도 비밀번호 확인)
- 로그인 없이 접근하는 공개 열람 화면 1개

### 만들지 않을 것 (비범위)
- 공유 링크 발급: 사이트 자체가 공개이므로 별도 공유 링크 발급 기능은 만들지 않는다.
- 회원가입, 로그인: 계정 개념은 만들지 않는다. 저장 기능만 .env의 비밀번호 1개(ADMIN_PASSWORD)로 잠그고, 가입 화면이나 사용자 테이블, user_id 칼럼은 두지 않는다.
- 폴더, 즐겨찾기, 수동 태그 편집: 분류는 AI 자동 태그 3개로만 처리한다.
- 모바일 앱, 브라우저 확장 프로그램: 웹 화면 하나만 만든다.
- 저장된 요약 재생성 버튼: AI는 저장 시점 1회만 호출한다는 규칙과 충돌하므로 만들지 않는다.
- 링크 미리보기 이미지(썸네일) 수집: 목록 카드는 텍스트만 보여준다.

---

## 7. 보안·개인정보 검토
*사용 문서의 공개등급, 개인정보 포함 여부, API 키 관리 방법을 적는다*

- 사용 문서의 공개등급: 공개(Public) 자료만 다룬다. 저장 대상은 누구나 접근 가능한 웹페이지의 URL과 그 요약이며, 사내 문서나 로그인이 필요한 페이지는 저장하지 않는다. 목록 화면 자체도 주소를 아는 사람에게 공개되므로, 남에게 보여도 무방한 링크만 저장한다.
- 개인정보 포함 여부: 개인정보를 수집하지 않는다. 회원가입이 없어 이름·이메일·비밀번호 등 사용자 정보를 저장하는 테이블이 없고, DB에는 링크·제목·요약·태그·저장일만 남는다. 단, URL 자체에 개인정보가 들어 있는 주소(예: 이메일이나 토큰이 포함된 링크)는 저장하지 않는다.
- 접속자 구분 값 취급: 비밀번호 5회 실패 차단은 접속자를 구분해야 하므로 IP를 쓴다. 그런데 IP는 다른 정보와 합치면 개인을 알아볼 수 있어 통상 개인정보로 다룬다. 그래서 IP 자체를 저장하지 않고, SESSION_SECRET으로 되돌릴 수 없게 변환한 값만 login_attempts에 남긴다. 같은 접속자는 같은 값이 나오므로 차단 기능은 그대로 동작하고, 저장된 값에서 원래 IP를 되찾을 수는 없다. 이 기록도 30일이 지나면 지운다(차단 중인 기록은 남긴다).
- API 키 관리 방법:
  - 모든 키는 .env 파일에만 두고, .gitignore에 .env를 등록해 GitHub에 올라가지 않게 한다.
  - OPENAI_API_KEY는 서버 쪽 코드(Next.js Route Handler)에서만 사용하고 브라우저로 내려보내지 않는다.
  - Supabase 연결에는 프로젝트 주소(SUPABASE_URL)와 서버 전용 키(SUPABASE_SERVICE_ROLE_KEY)를 .env에 추가해 사용한다. 기존의 SUPABASE_ACCESS_TOKEN은 계정 관리용이라 앱 연결에는 쓰지 않는다.
  - Supabase 키도 서버 쪽 코드에서만 사용하고 브라우저로 내려보내지 않는다.
  - 배포할 때는 .env를 올리는 대신 Vercel 환경변수에 같은 키를 따로 등록한다.
  - 통과 쿠키에 서명할 때는 ADMIN_PASSWORD가 아니라 별도의 SESSION_SECRET(무작위 32바이트)을 쓴다. 같은 값으로 서명하면 쿠키 하나가 새는 순간 그 안의 평문과 서명 쌍으로 비밀번호를 오프라인에서 역산할 수 있어, 5회 차단 규칙이 통째로 무력해지기 때문이다. SESSION_SECRET도 .env에만 두고 이 문서에는 값을 적지 않는다.
  - ADMIN_PASSWORD는 .env에만 두고 이 문서에는 값을 적지 않는다. 배포 전에 추측하기 어려운 값으로 반드시 교체한다. 4자리 숫자는 그 자체로는 약하므로 5회 실패 시 10분 차단 규칙과 함께 사용한다.
  - OpenAI 대시보드에서 월 사용 한도를 5달러로 설정해, 코드 오류나 외부의 반복 저장 시도로 요금이 무한정 늘어나지 않게 한다.

---

## 8. 기술 스택
*개발에 사용할 도구 모음 (이 과정에서는 Next.js로 고정)*

- Next.js — 화면과 서버 쪽 코드(API)를 한 프로젝트에서 처리한다. 언어는 TypeScript를 쓴다. (AI가 만든 코드에서 데이터 형태가 어긋날 때 실행 전에 드러나게 하기 위함)
- 고정 이유: Part 1~2 실습에서 쓴 것과 같은 구성이고, Part 5에서 Vercel로 배포할 예정이라 그대로 이어지게 하기 위함이다.
- 함께 쓰는 외부 서비스: 데이터 저장은 Supabase(9번 참고), 요약과 태그 생성은 OpenAI(5번 참고). 둘 다 Next.js의 서버 쪽 코드에서만 호출한다.
- 설치할 패키지: @supabase/supabase-js(DB 연결), openai(요약·태그 생성), cheerio(웹페이지에서 본문만 추출). 이 3개 외에 새 패키지를 더하지 않는다.
- 소스 코드는 GitHub 비공개 저장소로 관리하고, Vercel과 연결해 배포한다. .env는 올리지 않는다.

---

## 9. 데이터 구조
*저장한 링크를 어디에 어떤 형태로 담는지 적는다*

- 저장소: Supabase(PostgreSQL)에 links 테이블을 둔다. (차단 기록용 login_attempts 테이블은 아래 예외 항목 참고) 서버 파일이나 메모리에 저장하지 않는다. (Vercel 배포 시 저장 내용이 사라지기 때문)
- 접근 경로: DB 읽기와 쓰기는 모두 서버 쪽 코드(Next.js Route Handler)를 거친다. 브라우저에서 Supabase에 직접 접속하지 않는다. 테이블은 RLS(행 수준 보안)를 켜 두어 서버 전용 키로만 접근할 수 있게 한다. (브라우저가 DB에 직접 붙을 수 있으면 ADMIN_PASSWORD 확인을 우회해 저장과 삭제가 가능해지기 때문)
- links 테이블 칼럼
  - id: 자동 증가 번호 (기본키)
  - url: 저장한 원문 주소. 중복 저장을 막기 위해 고유값으로 둔다. 정규화 규칙은 호스트를 소문자로, 주소 끝의 슬래시와 # 뒤 조각을 제거하고, utm_로 시작하는 파라미터와 fbclid, gclid, ref를 제거하는 것이다.
  - title: 페이지 제목
  - summary: AI가 만든 3줄 요약
  - tags: AI가 만든 태그 최대 3개. 빈 값을 허용하지 않고 기본값은 빈 배열이며, AI를 호출하지 않은 경우(요약 실패, 요약 없음)에는 빈 배열로 저장한다. 쉼표로 이어붙인 문자열이 아니라 목록 형태(text 배열)로 저장한다. (태그 클릭 필터에서 email 같은 다른 태그가 딸려 나오지 않게 하기 위함)
  - created_at: 저장한 날짜와 시각. 시간대 정보를 포함해 저장하고(timestamptz), 화면에는 한국 시간(Asia/Seoul) 기준 2026-08-18 형태로 표시한다.
- 사용자 계정 정보는 저장하지 않으므로 user_id 칼럼은 두지 않는다.
- 예외 테이블: 비밀번호 5회 실패 차단을 기록하기 위해 login_attempts 테이블 1개만 추가로 둔다. (ip_hash, 실패 횟수, 마지막 실패 시각, 차단 해제 시각) 링크 데이터와 무관하며 사용자 계정 정보는 담지 않는다. 접속자 구분에는 IP를 그대로 쓰지 않고 되돌릴 수 없게 변환한 값(ip_hash)을 쓴다. 30일이 지난 기록은 지운다. (7번 참고)
- 검색 질의: tags가 배열이라 ILIKE를 직접 걸 수 없으므로, 제목·요약·태그를 함께 뒤지는 search_links(검색어, 태그, 시작 위치) 함수를 DB에 만들어 서버에서 호출한다.
- 설계 기준 규모: 한 달 100건 안팎, 1년이면 약 1,200건. 이 규모에서는 목록 20개씩 불러오기와 DB 텍스트 검색만으로 충분하므로 별도의 검색 엔진이나 캐시를 두지 않는다.

---

## 10. 개발 단위
*핵심 기능을 만들기 좋은 작은 단위로 나눠 만들 순서대로 적는다. 실제 작업 순서의 기준 문서는 PLAN.md이며, 아래 목록과 다르면 PLAN.md를 따른다.*

1. Next.js 프로젝트(TypeScript)를 만들고 빈 화면이 뜨는 것까지 확인한 뒤, git 저장소로 만들어 GitHub 비공개 저장소에 연결한다. (.env가 .gitignore에 들어 있는지 먼저 확인)
2. Supabase에 links 테이블을 만든다. (칼럼 6개, url은 고유값, RLS 켜기)
3. .env에 SUPABASE_URL과 SUPABASE_SERVICE_ROLE_KEY를 채우고, 서버 쪽 코드에서 테이블 조회가 되는지 확인한다.
4. 저장 API의 뼈대를 만든다. (비밀번호 확인 → 주소 형식 검사 → https 보완 → 추적 파라미터 제거 → 중복 확인 → URL만 저장, AI는 아직 붙이지 않음)
5. 저장 화면을 만든다. (주소 입력창, 비밀번호 입력칸, 저장 버튼, 처리 중에는 버튼 잠그고 "저장 중..." 표시)
6. 목록 조회 API를 만든다. (최신순 20개씩)
7. 목록 화면을 만든다. (격자 형태, 카드 컴포넌트 1개로 제목·요약·태그·저장일 표시)
8. 저장된 링크가 하나도 없을 때의 안내 문구를 넣는다.
9. 페이지 본문을 가져오는 부분을 만든다. (제목 추출, 15초 제한, 5,000자 자르기, 본문 200자 미만 판정)
10. AI 요약과 태그 생성을 붙인다. (gpt-4o-mini, 3문장 60자 이내, 태그 3개, 60자 초과 문장은 잘라서 저장)
11. 요약이 안 되는 경우를 처리한다. ("요약 실패", "요약 없음(본문이 짧은 페이지)"를 저장하고 카드에 표시)
12. 비밀번호 확인 처리를 완성한다. (통과하면 1시간짜리 서명 쿠키 발급, 5회 연속 실패 시 10분간 저장·삭제 차단)
13. 검색 API와 검색창을 만든다. (제목·요약·태그를 서버에서 함께 질의, 결과도 20개씩)
14. 카드의 태그를 클릭하면 그 태그만 걸러 보이게 한다.
15. 검색 결과가 0건일 때의 안내 문구와 검색 지우기 버튼을 넣는다.
16. "더 보기" 버튼으로 다음 20개를 이어서 불러오게 한다.
17. 카드에 삭제 버튼을 붙이고 삭제 API를 만든다. ("정말 삭제할까요?" 확인 후, 미통과 상태면 비밀번호도 함께 입력받아 확인)
18. 색상과 화면 폭을 정리한다. (CSS 변수 3개, 본문 1080px, 카드 3열에서 2열·1열로 줄어들게, 카드 높이 고정)
19. Vercel에 배포한다. (환경변수 등록, ADMIN_PASSWORD를 추측하기 어려운 값으로 교체, OpenAI 월 한도 5달러 설정)
