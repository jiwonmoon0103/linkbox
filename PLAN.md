# linkbox 개발 계획서 (PLAN)

- 작성자: 문지원
- 작성일: 2026-08-18
- 근거 문서: `PRD.md`, `prd_lite.md`, 작업 규칙: `CLAUDE.md`

## 이번 사이클 목표

`PRD.md`에 정의한 기능 전체를 만들어, **URL을 붙여넣으면 AI가 요약과 태그를 붙여 저장하고, 저장한 링크를 격자 목록에서 검색해 다시 찾을 수 있는 상태**까지 완성한다. 완성한 결과물은 Vercel에 배포해 주소로 접속할 수 있게 한다.

## 성공 기준

이 사이클은 아래 8개를 모두 만족하면 완료로 본다.

| # | 기준 | 확인 방법 |
|---|---|---|
| 1 | URL을 붙여넣으면 제목, 3줄 요약, 태그 1~3개가 붙어 저장된다 | 링크 5건을 저장해 카드 확인 |
| 2 | 저장한 링크가 격자 목록에 최신순으로 보인다 | 목록 화면 육안 확인 |
| 3 | 제목·요약·태그 어디에 있든 검색으로 찾을 수 있고, 21번째 이후 링크도 찾힌다 | 링크 25건 이상 저장 후 검색 |
| 4 | 비밀번호를 모르면 저장과 삭제가 되지 않는다 | 틀린 비밀번호로 시도 |
| 5 | 실패하는 링크(404, 유튜브 등)를 저장해도 앱이 멈추지 않고 "요약 실패"나 "요약 없음"으로 남는다 | 해당 링크 3종 저장 |
| 6 | 링크 1건당 AI 호출이 정확히 1회다 | OpenAI 대시보드 호출 수와 저장 건수 비교 |
| 7 | 링크 1건 저장 8초 이내, 목록 첫 화면 2초 이내 | 직접 시간 측정 (잠든 서버가 깨어나는 경우를 포함해 잰다) |
| 8 | 배포한 주소로 접속해 위 항목이 그대로 동작한다 | 배포 후 재확인 |

## 진행 상황 (2026-08-19 기준)

- **1~19번 전부 완료.** 각 작업은 검증 루프를 돌고 커밋까지 마쳤다. (git log 참고)
- **배포 완료.** 앱은 https://linkbox-green.vercel.app 에서 돌고 있고, 저장소는 https://github.com/jiwonmoon0103/linkbox (공개)다.
- **성공 기준 8개 전부 통과.** 판정 근거와 측정값은 `CHECK.md`에 있다.
- DB에는 실제로 저장한 링크 5건이 있다. 확인용으로 넣은 데이터는 남아 있지 않다.
- 남은 일: OpenAI 월 사용 한도 5달러 설정(작성자가 대시보드에서 직접), 보안 Low 4건과 DNS 리바인딩 방어. 셋 다 `CHECK.md` "남은 일"에 적혀 있다.

## 작업 순서

먼저 되면 나머지가 쉬워지는 것부터 정렬했다. 각 작업은 `CLAUDE.md`의 검증 루프를 한 바퀴 돈 뒤 다음으로 넘어간다.

1. Next.js(TypeScript) 프로젝트를 만들고 빈 화면을 띄운 뒤, git 저장소로 만들어 GitHub 저장소에 연결한다. 만드는 동안에는 비공개로 둔다. (완성 후 공개로 전환했다 — `PRD.md` 8번)
2. Supabase에 links 테이블과 login_attempts 테이블을 만들고, 제목·요약·태그를 함께 뒤지는 search_links 함수를 만든다. (칼럼 6개, url 고유값, tags는 text 배열이며 기본값은 빈 배열, RLS 켜기)
3. `.env`에 SUPABASE_URL과 SUPABASE_SERVICE_ROLE_KEY를 채우고, 서버 쪽 코드에서 테이블 조회가 되는지 확인한다.
4. 링크 데이터 타입과 고정 숫자 상수를 한 파일에 정의한다. (15초, 5,000자, 200자, 60자, 태그 3개, 20개)
5. 비밀번호 확인 공용 함수를 만든다. (일치 검사, 통과 시 1시간짜리 서명 쿠키 발급과 검증, 5회 실패 시 10분 차단을 login_attempts 테이블에 기록 — 저장과 삭제가 함께 쓴다)
6. 저장 API의 뼈대를 만든다. (비밀번호 확인 → 주소 형식 검사 → https 보완 → 추적 파라미터 제거 → 중복 확인 → URL만 저장)
7. 목록 조회 API를 만든다. (최신순 20개씩, 다음 20개를 이어서 요청할 수 있게)
8. 카드 컴포넌트 1개와 격자 레이아웃을 만든다. (CSS 변수 3색, 본문 1080px, 3열에서 2열·1열로, 카드 높이 고정)
9. 목록 화면에 조회 API를 연결하고, 링크가 0개일 때의 안내 문구를 넣는다.
10. 저장 화면을 만든다. (주소 입력창, 비밀번호 입력칸, 저장 버튼, 처리 중 버튼 잠금과 "저장 중..." 표시)
11. 페이지 본문을 가져오는 부분을 만든다. (title 추출, 15초 제한, 5,000자 자르기, 본문 200자 미만 판정)
12. AI 요약과 태그 생성을 붙인다. (gpt-4o-mini, 3문장 60자 이내, 태그 3개 공백 없이 10자, 60자 초과 문장은 60자에서 자르고 말줄임(…) 붙이기)
13. 요약이 안 되는 경우를 저장하고 카드에 표시한다. ("요약 실패", "요약 없음(본문이 짧은 페이지)")
14. 목록 API에 검색어·태그 파라미터를 더하고 검색창을 만든다. (제목·요약·태그를 ILIKE 부분 일치로 서버에서 질의, 결과도 20개씩)
15. 카드의 태그를 클릭하면 그 태그만 걸러 보이게 한다.
16. 검색 결과가 0건일 때의 안내 문구와 검색 지우기 버튼을 넣는다.
17. "더 보기" 버튼으로 다음 20개를 이어서 불러오게 한다.
18. 삭제 기능을 만든다. (카드의 삭제 버튼, "정말 삭제할까요?" 확인, 비밀번호 확인, 삭제 API)
19. 마무리 점검을 한다. (성공 기준 8개를 직접 측정하고, 고정 숫자 규칙이 코드에 그대로 지켜졌는지 확인)

여기까지가 이번 사이클의 작업이다.

**배포는 작성자가 직접 한다.** 개발 단위에서는 뺐지만, 배포할 때 잊으면 안 되는 것 세 가지를 적어 둔다.

- Vercel 환경변수에 `.env`와 같은 키를 등록한다. (`.env` 파일 자체는 올리지 않는다)
- `ADMIN_PASSWORD`를 추측하기 어려운 값으로 반드시 교체한다. (`PRD.md` 7번)
- OpenAI 대시보드에서 월 사용 한도를 5달러로 설정한다. (`PRD.md` 7번)

## 순서를 이렇게 잡은 이유

- **1~3번(토대)**: 프로젝트와 DB가 없으면 나머지 전부가 시작되지 않는다.
- **4~5번(공용 부품)**: 타입·상수와 비밀번호 확인은 6번 이후 거의 모든 작업이 가져다 쓴다. 먼저 만들면 뒤에서 같은 코드를 반복해서 짜지 않는다.
- **6~10번(AI 없이 완성되는 뼈대)**: 저장하면 목록에 뜨는 흐름을 AI 없이 먼저 완성한다. 여기까지가 앱의 골격이라, 이후 AI가 말썽을 부려도 앱 자체는 살아 있다.
- **11~13번(AI 붙이기)**: 본문 가져오기 → 요약 생성 → 실패 처리 순서다. 11번이 되면 12번은 붙이기만 하면 된다.
- **14~18번(찾기와 지우기)**: 링크가 쌓인 뒤에야 의미가 생기는 기능들이다.
- **19번(마무리)**: 만든 것이 성공 기준을 실제로 만족하는지 숫자로 확인한다. 배포는 작성자가 직접 하므로 개발 단위에서 뺐다.

## 이번 사이클에서 만들지 않는 것

`PRD.md` 6번 비범위와 동일하다. 공유 링크 발급, 회원가입·로그인, user_id 칼럼, 폴더·즐겨찾기·수동 태그 편집, 모바일 앱, 브라우저 확장, 요약 재생성 버튼, 썸네일 수집.

## 위험 요소

| 위험 | 대비 |
|---|---|
| AI가 요청하지 않은 기능을 덧붙여 범위가 늘어남 | 작업 단위마다 검증 루프 3단계에서 "요청 안 한 기능" 항목을 점검 |
| 비밀 키가 브라우저로 노출됨 | DB·AI 호출을 서버 코드로만 하고, 루프 3단계에서 매번 확인 |
| AI 호출이 중복되어 비용이 늘어남 | 저장 시점 1회 규칙 + OpenAI 월 한도 5달러 |
| 저장은 되는데 검색이 안 되는 상태로 진행 | 14번에서 링크 25건 이상 넣고 21번째 이후가 찾히는지 확인 |
