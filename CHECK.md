# linkbox 점검 결과 (CHECK)

- 작성일: 2026-08-19
- 대상 커밋: `f5ceb2e` (보안 점검 지적 4건 수정)
- 근거 문서: `PLAN.md` 성공 기준, `DESIGN.md` 설계, 배포 전 보안 점검
- 점검 방식: 실제 앱을 띄워 시나리오를 실행하고 측정. 코드 읽기만으로 판정하지 않음.

---

## 판정: **실패**

성공 기준 8개 중 **5개 통과, 2개 미달, 1개 미확인**. 전부 만족해야 완료이므로 아직 통과가 아니다.

| 구분 | 결과 |
|---|---|
| 성공 기준 | 5 통과 / 2 미달 / 1 미확인 |
| 설계 대비 차이 | 11건 중 **1건 수정 완료**, 10건 남음 (대부분 넘어가기로 결정) |
| 보안 | Critical 1건·High 3건·Medium 5건 발견 → **5건 수정 완료**, 나머지 남음 |

**미달 2건은 기능이 깨진 것이 아니라 기준선에 못 미친 것이다.** 앱은 정상 동작한다.

---

## 1. 성공 기준 판정

`PLAN.md`의 8개 기준을 실제로 실행해 측정했다. (링크 8건 저장 + 28건 상태에서 검색)

| # | 기준 | 판정 | 근거 |
|---|---|---|---|
| 1 | 제목·3줄 요약·태그 1~3개가 붙어 저장 | **통과** | 5건 모두 3문장·태그 3개. 최장 문장 48자 (상한 60자) |
| 2 | 격자 목록에 최신순 | **통과** | 3열, 본문 1080px, 카드 높이 8장 모두 240px |
| 3 | 제목·요약·태그로 검색, 21번째 이후도 | **통과** | 실제 저장분을 21~28번째로 밀어낸 뒤 4가지 경로로 모두 검색됨 |
| 4 | 비밀번호 모르면 저장·삭제 불가 | **통과** | 둘 다 401, 링크 수 변화 없음 |
| 5 | 실패 링크 3종에도 앱이 안 멈춤 | **미달** | 404·PDF는 정상. **유튜브가 AI 요약을 만듦** |
| 6 | 링크 1건당 AI 호출 1회 | **통과** | 4건 중 AI 대상 1건 → 총 1회 (호출 수 계측) |
| 7 | 저장 5초 / 목록 2초 | **미달** | 목록 0.11초 통과. **저장 최대 5.7초** |
| 8 | 배포 주소에서 동작 | **미확인** | 아직 배포 전 |

### 미달 상세

**기준 5 — 유튜브**
페이지에서 걷어낸 글자가 339자로 200자 기준을 넘어, 규칙상 AI를 부르게 된다. 그 결과 메뉴 글자를 근거로 "Rick Astley 곡 정보" 같은 요약이 만들어진다. 기준은 "요약 실패"나 "요약 없음"으로 남기를 요구한다. **앱이 멈추지는 않는다.**

**기준 7 — 저장 시간**

| 링크 | 시간 |
|---|---|
| PDF | 0.6초 |
| 404 | 1.3초 |
| MDN / Next.js 문서 | 2.3~2.5초 |
| 유튜브 | 3.2초 |
| PostgreSQL 문서 | 3.4초 |
| 위키 Machine learning | 4.2초 |
| **위키 Kimchi** | **5.7초** |

본문이 긴 페이지는 가져오기(약 1초)에 AI가 5,000자를 읽고 3문장을 만드는 시간이 붙어 5초를 넘는다. 8건 중 1건에서 발생했다.

> 측정 시점은 `14169e0`이다. 이후 커밋(`c8f6015`, `f5ceb2e`)은 저장·목록 경로의 처리량을 바꾸지 않으며, 저장·삭제가 정상 동작함은 `f5ceb2e` 이후 다시 확인했다.

---

## 2. 설계(DESIGN.md) 대비 차이

| # | 항목 | 상태 |
|---|---|---|
| 1 | 저장 후 새 카드가 목록에 안 나타남 | **수정 완료** (`14169e0`) |
| 2 | 유튜브가 "요약 없음"이 아니라 AI 요약을 만듦 | 남음 — 넘어가기로 결정 |
| 3 | 중복 저장 시 기존 카드를 보여주지 않음 | 남음 — 넘어가기로 결정 |
| 4 | 목록 조회 실패 시 안내 화면 없음 | 남음 — 넘어가기로 결정 |
| 5 | "더 보기" 실패 시 아무 표시 없음 | 남음 — 넘어가기로 결정 |
| 6 | 삭제 후 "더 보기" 버튼 상태를 다시 계산하지 않음 | 남음 — 넘어가기로 결정 |
| 7 | 문서가 "세 기능 모두 비밀번호 필요"라고 적었으나 목록은 공개 | 남음 — **문서가 틀림** |
| 8 | 검색 줄 `[✕ 지우기]`가 스케치에만 있고 상세 표엔 없음 | 남음 — **문서끼리 불일치** |
| 9 | `search_links` 인자가 문서 3개, 실제 4개 | 남음 — **문서가 틀림** |
| 10 | 중복(409) 응답이 `error`와 `data`를 함께 내리는데 형식 표에 없음 | 남음 — **문서가 틀림** |
| 11 | 저장 5초 초과 | 남음 — 성공 기준 7과 같은 건 |

2~6번은 "설계와 달라도 정상 동작하면 넘어간다"는 결정에 따라 남겨 두었다.
7~10번은 코드가 아니라 `DESIGN.md`를 고쳐야 맞는 항목이며, 수정하지 않기로 결정했다.

**주의**: 7~10번 때문에 `DESIGN.md`에는 사실과 다른 내용이 남아 있다. 이 문서를 처음 읽는 사람은 오해할 수 있다.

또한 1번 수정으로 생긴 동작 변화가 하나 있다 — **"더 보기"로 펼친 상태에서 링크를 저장하면 목록이 첫 20개로 돌아간다.** 새 카드가 맨 앞에 오면서 뒷번호가 밀려 경계에서 링크가 겹치거나 빠지는 것을 막기 위한 선택이다.

---

## 3. 보안 점검

배포 전 점검에서 발견한 항목과 처리 상태다.

| 등급 | 항목 | 상태 |
|---|---|---|
| Critical | 실패 횟수 세기가 동시 요청에 무력화됨 | **수정 완료** (`c8f6015`) |
| High | SSRF — 사용자가 준 주소로 내부망 요청 | **남음** |
| High | 쿠키 서명 키가 `ADMIN_PASSWORD` 자체 | **수정 완료** (`f5ceb2e`) |
| High | 제목만 길이 제한이 없음 | **수정 완료** (`f5ceb2e`) |
| High | 프롬프트 인젝션 대비 없음 | **남음** |
| Medium | 응답 크기 상한 없음 | **남음** |
| Medium | 보안 헤더 없음 | **수정 완료** (`f5ceb2e`) |
| Medium | 공개 GET API에 입력 상한 없음 | **남음** |
| Medium | `login_attempts`의 IP가 무기한 보관 | **남음** |
| Medium | 검색엔진 색인 차단 없음 | **수정 완료** (`f5ceb2e`) |
| Low | `server-only` 가드 없음 외 5건 | **남음** |

### 수정 확인

| 항목 | 확인 방법과 결과 |
|---|---|
| 실패 횟수 원자성 | 동시 30건: 수정 전 401×27·차단 안 됨 → 수정 후 **429×27·차단됨** |
| 쿠키 서명 키 | 예전 방식(비밀번호로 서명)한 위조 쿠키 → **401 거절** |
| 제목 길이 | title 10만 자 페이지 → **200자로 잘려 저장** |
| 보안 헤더 | 응답 헤더 5종 확인 (`X-Frame-Options: DENY` 외) |
| 색인 차단 | `<meta name="robots" content="noindex, nofollow">` |

### 문제없음이 확인된 것

비밀 키 노출 없음(빌드 산출물·git 히스토리 모두 0건), XSS 없음(React 이스케이프, `dangerouslySetInnerHTML` 0건, `javascript:` URL 차단), SQL 인젝션 없음(RPC 파라미터 바인딩), RLS 정상, CSRF 실질 방어됨, 에러 메시지에 내부 정보 없음, AI 호출 1회 규칙 유지(인증이 AI 호출보다 먼저).

### 점검 보고 중 정정한 것

보안 점검은 "보안 헤더 없음"을 클릭재킹으로 삭제를 유발할 수 있다고 보았으나, **쿠키가 `SameSite=Lax`라 교차 사이트 iframe에서는 쿠키가 실리지 않아 그 시나리오는 성립하지 않는다.** 헤더 추가는 여전히 옳지만 심각도는 그보다 낮다.

---

## 4. 고쳐야 할 항목 (우선순위 순)

| 순위 | 항목 | 왜 중요한가 | 드는 품 |
|---|---|---|---|
| 1 | **SSRF 방어** | 지금도 `http://127.0.0.1:...`, `http://169.254.169.254/`를 저장하면 서버가 그 주소로 요청을 보내고, **공개 목록에 그 요약이 실린다.** 리다이렉트를 따라가므로 입력 검사만으로는 막히지 않는다 | 큼 (홉마다 DNS 조회 후 IP 대역 검사) |
| 2 | **`ADMIN_PASSWORD` 강화** | 4자리 숫자면 여러 IP로 나눠 시도할 때 여전히 약하다. 낱말 3개(`고양이우산바다7`)만 이어붙여도 충분하다 | 없음 (값만 교체) |
| 3 | **유튜브 판정** (기준 5) | 성공 기준 미달 항목. 메뉴 글자로 요약을 지어낸다 | 중간 (`<main>`/`<article>` 우선 추출로 완화 가능) |
| 4 | **저장 5초 초과** (기준 7) | 성공 기준 미달 항목. 8건 중 1건 | 중간 (본문 길이를 줄이거나 기준을 재검토) |
| 5 | **응답 크기 상한** | 무한 스트림을 뱉는 주소 하나로 서버가 메모리 부족으로 죽을 수 있다 | 중간 |
| 6 | **공개 GET API 입력 상한** | 인증이 없어 누구나 `q`·`offset`으로 DB를 태울 수 있다 | 중간 |
| 7 | **IP 보관 기간** | IP는 통상 개인정보로 다뤄지는데 무기한 쌓인다. `PRD.md`는 "개인정보를 수집하지 않는다"고 적혀 있어 문서와도 어긋난다 | 중간 (`PRD.md` 수정 + 정리 작업) |
| 8 | **프롬프트 인젝션 완화** | 악성 페이지가 요약을 조작해 공개 목록에 실을 수 있다. 근본 차단은 불가하나 구분자와 지시로 완화된다 | 작음 |
| 9 | **`server-only` 가드** | 지금은 유출 경로가 없지만, 나중에 실수로 클라이언트가 서버 모듈을 가져가면 빌드가 통과해버린다 | 작음 (세 줄) |
| 10 | **`DESIGN.md` 문서 오류 4건** | 코드는 정상. 문서가 사실과 달라 다음 작업자가 오해할 수 있다 | 작음 |
| 11 | 나머지 설계 차이 (중복 카드 강조, 조회 실패 화면, 더 보기 실패 알림, 삭제 후 버튼 상태) | 넘어가기로 한 항목들 | 작음 |

### 배포 전에 최소한 할 것

1. **`ADMIN_PASSWORD` 교체** (순위 2) — 값만 바꾸면 된다
2. Vercel 환경변수에 `.env`와 같은 키 등록 (**`SESSION_SECRET`이 새로 추가되었다**)
3. OpenAI 월 사용 한도 5달러 설정
4. 배포 후 성공 기준 8번 확인

`SSRF`(순위 1)는 배포 전에 하는 것이 가장 안전하지만, 링크를 저장할 수 있는 사람이 비밀번호를 아는 사람으로 제한되어 있으므로 배포 후 과제로 미룰 수도 있다. 다만 저장 권한을 여러 사람과 나눌 계획이라면 먼저 처리하는 편이 좋다.

---

## 5. 이 문서를 다시 만드는 방법

이 문서는 아래 세 가지를 실제로 실행해 얻은 결과를 정리한 것이다.

1. `PLAN.md` 성공 기준 8개 시나리오 실행
2. `DESIGN.md`와 구현 코드 대조
3. 배포 전 보안 점검

코드가 바뀌면 이 문서는 낡는다. 다시 점검할 때는 대상 커밋을 맨 위에 적고 덮어쓴다.
