# CLAUDE.md — linkbox 프로젝트 규칙서

이 파일은 Claude Code가 이 폴더에서 작업할 때 지켜야 할 규칙을 적은 문서다.

## 프로젝트 한 줄 요약

나중에 볼 링크를 붙여넣기만 하면 AI가 요약과 태그를 붙여 정리해주는 개인용 링크 보관함.

## 문서

- `PRD.md` — 정식 기획서. 모든 결정의 근거다. 구현 전에 반드시 읽는다. 문서끼리 어긋나면 PRD.md가 우선하고, 화면 디자인에 한해서만 prd_lite.md가 원본이다.
- `PLAN.md` — 작업 순서와 성공 기준. 무엇을 몇 번째로 만들지는 이 문서를 따른다.
- `DESIGN.md` — 화면 구성, 데이터 흐름, 파일 구조. 코드를 짜기 전에 해당 부분을 읽는다.
- `prd_lite.md` — 가벼운 기획서. 화면 디자인(색상, 격자, 폭)의 원본이다.
- `CHECK.md` — 점검 결과. 성공 기준 판정, 설계 대비 차이, 보안 점검, 남은 일이 있다. **지금 무엇이 끝났고 무엇이 남았는지는 여기부터 본다.** 코드가 바뀌면 낡으므로, 다시 점검할 때 대상 커밋을 적고 덮어쓴다.
- `README.md` — 저장소를 처음 보는 사람에게 보여줄 소개. 저장소가 공개이므로 비밀 값이나 그 형태를 적지 않는다.
- 위 문서와 어긋나는 코드를 짜지 않는다. 어긋나야 할 이유가 생기면 코드를 고치기 전에 먼저 물어본다.

## 현재 상태 (2026-08-19)

- **첫 사이클이 끝났다.** `PLAN.md` 작업 순서 1~19번을 모두 만들었고 배포까지 마쳤다. 성공 기준 8개는 전부 통과다.
- 앱 https://linkbox-green.vercel.app / 저장소 https://github.com/jiwonmoon0103/linkbox (공개)
- **무엇이 끝났고 무엇이 남았는지는 `CHECK.md`부터 본다.** 남은 일도 그 문서 "남은 일"에 있다.
- 새 작업을 시작하기 전에 `PLAN.md`의 "진행 상황"으로 지금 위치를 확인한다. (PRD.md 10번 목록과 다르면 PLAN.md가 기준이다)
- 한 번에 한 단위씩 만들고, 동작을 확인한 뒤 다음 단위로 넘어간다.
- 이미 돌아가는 앱이므로, 고치기 전에 지금 동작을 먼저 확인한다. 개발 서버가 3000번에 이미 떠 있을 수 있으니 새로 띄우기 전에 확인한다.

## 기술 스택

- Next.js + TypeScript — 화면과 API를 한 프로젝트에서 처리한다.
- Supabase (PostgreSQL) — links 테이블 1개
- OpenAI gpt-4o-mini — 요약과 태그 생성
- 배포는 Vercel을 사용한다.
- 이 구성은 `PRD.md`에 고정된 것이다. 다른 프레임워크로 바꾸거나 마이그레이션을 제안하지 않는다.

## 절대 하지 말 것

- 브라우저에서 Supabase에 직접 접속하지 않는다. DB 읽기와 쓰기는 모두 서버 쪽 코드(Route Handler)를 거친다.
- `SUPABASE_SERVICE_ROLE_KEY`와 `OPENAI_API_KEY`를 브라우저로 내려보내지 않는다. `NEXT_PUBLIC_` 접두사를 붙이지 않는다.
- 링크 1건당 AI 호출은 1회를 넘지 않는다. 목록 조회, 검색, 재요약으로 AI를 부르지 않는다.
- `PRD.md` 6번의 비범위 항목을 만들지 않는다: 공유 링크 발급, 회원가입/로그인, user_id 칼럼, 폴더/즐겨찾기/수동 태그 편집, 모바일 앱, 브라우저 확장, 요약 재생성 버튼, 썸네일 수집.
- 요청받지 않은 기능을 덧붙이지 않는다. 필요해 보이면 만들지 말고 먼저 물어본다.
- 파일을 바로 삭제하지 않는다. `trash-can` 폴더로 옮겨만 두고, 사용자가 확인하기 전에는 그 안의 파일을 지우지 않는다.

## 비밀 정보 취급

- `.env` 등 비밀 정보 파일과 `node_modules`, `trash-can` 폴더는 `.gitignore`에 등록해 두고 절대 커밋하지 않는다.
- 외부 서비스 인증이 필요하면 토큰 값을 사용자에게 묻거나 채팅에 출력하지 않는다. `.env`에 있는 값을 읽어서 사용한다.
  - Supabase 작업이 필요하면 Supabase CLI를 설치해 `.env`의 `SUPABASE_ACCESS_TOKEN`으로 작업한다.
  - Vercel 작업(배포 등)이 필요하면 Vercel CLI를 설치해 `.env`의 `VERCEL_TOKEN`으로 인증해 작업한다.
- 토큰 값 자체를 파일이나 로그, 커밋 메시지에 남기지 않는다.

### 환경변수 목록

`.env`에 다음 키를 둔다. 값은 이 문서에 적지 않는다.

- `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` — 서버 전용
- `OPENAI_API_KEY` — 서버 전용
- `ADMIN_PASSWORD` — 저장/삭제 확인용
- `SESSION_SECRET` — 통과 쿠키 서명 전용 키. `ADMIN_PASSWORD`와 반드시 다른 값을 쓴다. (같은 값을 쓰면 쿠키 하나가 새었을 때 비밀번호를 역산할 수 있다)
- `SUPABASE_ACCESS_TOKEN`, `VERCEL_TOKEN`, `GITHUB_TOKEN` — 계정 관리용. 앱 코드에서 쓰지 않는다.

## 저장 처리 규칙 (숫자로 고정된 것)

- 페이지 가져오기는 15초를 넘기면 중단하고 "요약 실패"로 저장한다.
- 본문은 앞에서부터 5,000자까지만 AI에 넘긴다.
- 본문이 200자 미만이면 AI를 호출하지 않고 "요약 없음(본문이 짧은 페이지)"으로 저장한다.
- 요약은 한국어 3문장 이내, 각 문장 60자 이내. 넘치면 잘라서 저장하고 다시 생성하지 않는다.
- 태그는 최대 3개, 각 태그는 공백 없이 10자 이내, 영어는 소문자로 통일한다.
- 제목은 페이지의 title을 그대로 쓴다. 비어 있거나 의미가 없을 때만 AI가 짓는다.
- 페이지를 읽지 못하거나 실패해도 URL과 제목은 저장한다. 요약을 지어내지 않는다.

## 저장 권한 규칙

- 저장과 삭제는 `.env`의 `ADMIN_PASSWORD`가 일치할 때만 처리한다. 불일치면 AI를 호출하지 않는다.
- 한 번 통과하면 서버가 서명한 쿠키(HttpOnly)를 1시간 만료로 발급해 그동안 다시 묻지 않는다. 브라우저에 남은 표시만으로 통과시키지 않는다. 계정이나 사용자 테이블은 만들지 않는다.
- 5회 연속 실패하면 10분간 차단한다. 저장과 삭제 모두에 적용하며, IP 기준으로 login_attempts 테이블에 기록한다.
- 삭제 직전에 확인을 한 번 받는다.

## 목록과 검색 규칙

- 목록은 최신순 20개씩. 나머지는 "더 보기"로 가져온다.
- 검색과 태그 필터는 서버에서 DB에 질의한다. 이미 불러온 20개 안에서 거르지 않는다.
- 검색은 제목, 요약, 태그 세 곳을 함께 뒤진다. ILIKE 부분 일치를 쓰고 대소문자를 구분하지 않는다.
- 저장일은 한국 시간(Asia/Seoul) 기준 `2026-08-18` 형태로 표시한다. 상대 표기를 쓰지 않는다.
- 카드를 클릭하면 원문을 새 탭에서 연다.
- 빈 화면 문구는 "링크 0개"와 "검색 결과 0건"을 구분해서 보여준다.

## 데이터 구조

`links` 테이블과 차단 기록용 `login_attempts` 테이블을 둔다. 검색은 `search_links` 함수로 처리한다. RLS를 켜고 서버 전용 키로만 접근한다.

| 칼럼 | 내용 |
|---|---|
| `id` | 자동 증가 번호 (기본키) |
| `url` | 추적 파라미터를 제거한 주소. 고유값(중복 저장 차단) |
| `title` | 페이지 제목 |
| `summary` | AI 3줄 요약 |
| `tags` | text 배열 (쉼표 문자열 아님) |
| `created_at` | 저장 시각 |

## 화면 규칙

- 카드를 격자로 나열한다. 본문 최대 폭 1080px, 가운데 정렬. 기본 3열이고 좁아지면 2열, 1열로 줄인다.
- 카드 한 칸의 최소 폭은 280px, 높이는 모두 같게 고정한다.
- 목록 카드는 컴포넌트 1개로만 만든다.
- 색은 3개를 넘기지 않고 CSS 변수로 한 곳에서 관리한다. 배경 흰색, 글자 `#1F2937`, 강조 `#2563EB`.
- 애니메이션과 그림자는 쓰지 않는다. 링크가 0개일 때 보여줄 일러스트 1장만 예외로 허용한다.

## 작업 절차 (검증 루프)

작업 단위 하나마다 아래 1~4를 통과할 때까지 반복한다. 한 바퀴를 돌지 않은 변경은 끝난 것으로 보지 않는다.

1. **변경한다** — 지금 하는 작업이 `PLAN.md`의 몇 번 작업인지 먼저 말하고, 그 단위에 필요한 만큼만 고친다.
2. **결과를 직접 확인한다** — 코드를 읽는 것으로 끝내지 않는다. 개발 서버를 띄워 브라우저로 화면을 열어보거나, 해당 명령을 실제로 실행해 동작을 눈으로 본다. 서버 로그와 브라우저 콘솔에 에러가 없는지도 함께 본다.
3. **스스로 코드 리뷰한다** — 아래를 점검한다. 필요하면 서브에이전트를 부른다.
   - `PRD.md`, `prd_lite.md`와 어긋난 곳이 없는가
   - 비밀 키가 브라우저로 나가지 않는가, 브라우저에서 DB에 직접 붙지 않는가
   - 링크 1건당 AI 호출이 1회를 넘지 않는가
   - 숫자로 고정한 규칙(15초, 5,000자, 200자, 3문장 60자, 태그 3개, 20개)을 지켰는가
   - 요청받지 않은 기능이 끼어들지 않았는가
4. **문제가 있으면 고치고 1)로 돌아간다** — 원인을 확인한 뒤 한 곳씩 고친다. 추측으로 여러 곳을 동시에 바꾸지 않는다.

통과하면 **무엇을 왜 바꿨는지 한 줄로 요약**하고 멈춘다. 사용자 확인을 받은 뒤 다음 단위로 넘어간다.

### 확인할 때 지킬 것 (겪어보고 정한 것)

- `scripts/`의 확인용 스크립트는 `node --conditions=react-server --env-file=.env scripts/....mts`로 실행한다. `lib/`에 `server-only`가 걸려 있어 이 조건이 없으면 스크립트가 바로 죽는다.
- 확인하려고 넣은 링크와 차단 기록은 끝나면 **반드시 지워** 원래 상태(링크 0건)로 되돌린다. 시험 데이터를 남긴 채로 끝내지 않는다.
- 확인 결과가 이상하면 앱을 고치기 전에 **확인 방법이 낡았는지 먼저 본다.** (브라우저 자동 클릭이 헛나가거나, 바깥 시험용 사이트가 들쭉날쭉하거나, 규칙을 DB로 옮긴 뒤 스크립트만 옛 계산을 쓰는 일이 있었다)
- 라우트나 DB 스키마를 새로 만들면 `npm run build`를 한 번 돌려 타입을 다시 만든 뒤 `tsc`를 본다. 빌드 전에는 새 라우트 타입이 없어 헛된 타입 오류가 난다.

## 작업 방식

- 모든 설명과 코드 주석은 한국어로 작성한다.
- 새 파일은 `my-app` 폴더 안에만 만든다. 폴더 밖에 파일을 만들지 않는다.
- 코드를 바꾸면 반드시 무엇을 왜 바꿨는지 한 줄로 알려준다.
- 코드를 짜기 전에 어떤 개발 단위를 하는 중인지 말한다.
- 큰 변경을 한꺼번에 하지 않는다. 한 단위가 끝나면 멈추고 확인을 받는다.
- 설계가 `PRD.md`와 달라져야 하면 코드보다 문서를 먼저 고친다.
- 에러가 나면 추측으로 여러 곳을 고치지 말고, 원인을 확인한 뒤 한 곳을 고친다.
- 이미 설치된 서브에이전트를 필요할 때마다 적극 활용한다. (코드 품질 점검, 기획서와 구현 차이 확인, 테스트, 보안 검토 등)
